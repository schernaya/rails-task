<p id="notice"><%= notice %></p>

<h1>Books</h1>

<%#= form_with model: @book do |f| %>
<%= form_tag(books_path, method: :get) do %>
  <%= select_tag :search, author_names_for_select, multiple: 'true' %>
  <%#= select_tag :search, author_names_for_select, { selected: '0' }, multiple: 'true' %>
  <%= submit_tag "Search" %>
  <%#= f.select :search, options_for_select(Author.all.order(:name => :asc).map {|p| [ p.name ]}.uniq), {:include_blank => "Choose authors:", :multiple => true} %>
  <%#= f.select :search, author_names_for_select, { selected: '0' }, multiple: 'true' %>
  <%#= f.submit "Search" %>
<% end %>

<%#= form_tag(books_path, :method => "get", id: "search-form") do %>
    <%#= label :search, "Search" %>
    <%#= select_tag :search, options_for_select(Author.all.order(:name => :asc).map {|p| [ p.name ]}.uniq), {:include_blank => "Choose authors:", :multiple => true} %>
  <%#= submit_tag "Search" %>
<%# end %>

<table>
  <!--  <thead>-->
  <!--    <tr>-->
  <!--      <th>Title</th>-->
  <!--      <th colspan="3"></th>-->
  <!--    </tr>-->
  <!--  </thead>-->

  <tbody>
  <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= link_to 'Show', book %></td>
      <td><%= link_to 'Edit', edit_book_path(book) %></td>
      <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
